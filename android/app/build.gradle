    plugins {
    id "com.android.application"
    id "com.google.gms.google-services" // FlutterFire Configuration
    id "kotlin-android"
    id "dev.flutter.flutter-gradle-plugin" // Flutter Gradle Plugin
}

android {
    namespace = "com.mike.proyect_atenea"
    compileSdk = 34 // Cambia a la versión más reciente disponible

    defaultConfig {
        applicationId = "com.mike.proyect_atenea"
        minSdkVersion 23
        targetSdkVersion 34
        versionCode 1
        versionName "1.0"
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }

    kotlinOptions {
        jvmTarget = "1.8"
    }

    signingConfigs {
        /*
        
        release {
            storeFile file('C:/Users/Michael Noguera/Documents/project_atenea/temp-key.jks')
            storePassword 'quesueno2121' // Contraseña del keystore
            keyAlias 'temp-key' // Alias
            keyPassword 'quesueno2121' // Contraseña del alias
        }
        */
    }

    buildTypes {
        release {
            //signingConfig signingConfigs.release
            minifyEnabled true // Activa ProGuard para optimizar el APK
            shrinkResources true // Elimina recursos no utilizados
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
        debug {
            //signingConfig signingConfigs.release // Usa el keystore también para debug si lo necesitas
            minifyEnabled false
            shrinkResources false
        }
    }
}

flutter {
    source = "../.."
}


apply plugin: 'com.google.gms.google-services'